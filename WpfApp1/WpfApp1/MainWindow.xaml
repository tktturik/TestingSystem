<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfApp1"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainViewModel />
    </Window.DataContext>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Button Command="{Binding ShowCreateTestViewCommand}" Grid.Column="0" 
                Width="50" Height="50" 
                Content="Create" 
                Margin="10,6,10,74" Grid.Row="1" />

        <Button Command="{Binding ShowTakeTestViewCommand}" 
                Grid.Row="1" Grid.Column="0" 
                Width="50" Height="50" 
                Content="Take" 
                Margin="10,70,10,10" />

        <Grid Grid.Row="0" Grid.RowSpan="3" Grid.Column="1" Margin="10">
            <ContentControl Content="{Binding UserControl}"/>
        </Grid>
    </Grid>
</Window>
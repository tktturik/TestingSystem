<UserControl x:Class="testingSystem.View.UserControls.AuthModalWindow"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:testingSystem.View.UserControls"
             xmlns:vm="clr-namespace:testingSystem.ViewModels"
             xmlns:hc="https://handyorg.github.io/handycontrol"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:AuthVM />
    </UserControl.DataContext>
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">


        <Border BorderThickness="3" CornerRadius="10" BorderBrush="Purple" Background="{StaticResource PrimaryBrush2}">
            <Grid >
                <StackPanel Orientation="Vertical" Margin="10">
                    <Image Source="/Image/user.png" Style="{StaticResource ImageStyle2}"></Image>
                    <TextBlock Style="{StaticResource TextStyle}" Text="Вход"></TextBlock>

                    <hc:AutoCompleteTextBox Width="200" VerticalAlignment="Center" 
                                            HorizontalAlignment="Center" Margin="32" 
                                            ItemsSource="{Binding Users}" 
                                            Text="{Binding SearchText}" 
                                            SelectedItem="{Binding SelectedUserValue, Mode=TwoWay}"
                                            
                                            
                                            
 />
                    

                    <Button Style="{StaticResource CreateTestBtn}" Command="{Binding StartButtonCommand}" IsTabStop="True" TabIndex="2">
                        <TextBlock Style="{StaticResource TextStyleBtn}" Text="Продолжить"></TextBlock>
                    </Button>
                </StackPanel>

            </Grid>
        </Border>
    </Grid>
</UserControl>
